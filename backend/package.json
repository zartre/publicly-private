{
    "name": "backend",
    "scripts": {
        "dev": "bun run --hot src/index.ts",
        "generate-keys": "bun run src/generate-keys.ts",
        "test": "bun run src/test-client.ts",
        "extract-keys": "bun run extract-keys-for-frontend.ts",
        "verify": "bun run --eval \"import { getKeysFromEnv } from './src/crypto'; try { getKeysFromEnv(); console.log('✅ Backend setup verified! Keys loaded successfully.'); } catch (e) { console.error('❌ Setup incomplete:', e.message); process.exit(1); }\"",
        "example": "bun run example-client.ts"
    },
    "dependencies": {
        "hono": "^4.10.3"
    },
    "devDependencies": {
        "@types/bun": "latest"
    }
}
